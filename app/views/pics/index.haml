%h1 Pics Index

%strong Share your life!
%button=link_to "Add new pic", new_pic_path
.container
- @pics.each do |pic|
	%p= link_to (image_tag pic.user.avatar.url(:tiny)), profile_path(pic.user)
	%h3= pic.title
	%h4= pic.user.handle
	- if pic.image.present?
		%div= link_to (image_tag pic.image.url(:medium)), pic
	%p= "#{time_ago_in_words(pic.created_at)} ago"

	- if pic.user == current_user
		= link_to "Edit", edit_pic_path(pic)
		|
		= link_to "Delete", pic_path(pic), method: :delete, data: { confirm: 'Are you sure you want to delete this Post?'}